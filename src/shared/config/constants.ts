// Глобальные UI-константы (FSD: shared/config).
// Здесь живут безопасные для повторного использования числовые/строковые значения,
// не зависящие от бизнес-логики конкретного слайса.

/** Размер страницы для основных списков (поиск, каталоги). */
export const PAGE_SIZE_DEFAULT = 25;

/** Размер страницы для списка партнёров (карточки шире — строк меньше). */
export const PAGE_SIZE_PARTNERS = 20;

/** Размер страницы для таблицы документов в админке. */
export const PAGE_SIZE_DOCUMENTS = 15;
